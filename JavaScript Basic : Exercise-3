Write a JavaScript program to get the current date.
Expected Output :
mm-dd-yyyy, mm/dd/yyyy or dd-mm-yyyy, dd/mm/yyyy

var d = new Date();  
var n = d.getDate();
var m = d.getMonth()+1;
var y = d.getFullYear();
  
if (m<10)
  {
    mo = "0" + m;
  }

document.write(n + "/" + mo + "/" + y);
